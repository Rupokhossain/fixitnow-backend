enum Role {
    CUSTOMER
    TECHNICIAN
    ADMIN
}

enum Status {
    ACTIVE
    BLOCKED
}

enum Provider {
    STRIPE
    SSLCOMMERZ
}

enum PaymentStatus {
    PENDING
    PAID
    FAILED
}

enum BookingStatus {
    REQUESTED
    ACCEPTED
    DECLINED
    PAID
    IN_PROGRESS
    COMPLETED
}